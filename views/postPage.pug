extends layouts/main-layout.pug

block content 
    script. 
        const postId = '!{postId}'
    .postsContainer

    +createReplyModal(userLoggedIn)

block scripts 
    script(src="/js/postPage.js") 